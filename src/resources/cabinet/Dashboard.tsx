import { Card, CardContent, CardHeader } from "@mui/material";

export const Dashboard = () => (
    <Card>
        <CardHeader title="Welcome!" />
        <CardContent>Welcome to administrator's cabinet</CardContent>
    </Card>
);